When updating the model or starting from fresh, run
```yarn profile```
to create tfjs config for optimization tensorflowjs bandle
Then run 
```yarn optimizeTF```
to create optimized TFJS bundles which will be import using alias